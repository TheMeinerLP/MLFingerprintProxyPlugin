package dev.themeinerlp.mlfingerprint;

/**
 * Enum representing the direction of a packet.
 * This is used to distinguish between incoming and outgoing packets.
 */
enum Direction {
    INCOMING,
    OUTGOING
}
